enum PaticipantWithEventsAction {
  removeParticipantFromEvent,
  addParticipantToEvent,
}

enum ParticpantsStatus { initial, loading, success,changed, failed,loaded,empty,sent,ToMember,loadingExcel }
enum GuestStatus { initial, loading, success,changed, failed,loaded,loadingExcel,ToMember, }
